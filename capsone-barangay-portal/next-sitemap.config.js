/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://barangayfairview.site', // 👈 change this to your actual domain
  generateRobotsTxt: true, // Generates robots.txt as well
  sitemapSize: 5000,
};
